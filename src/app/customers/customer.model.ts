export interface CustomerModel {
    _id: string
    documentType: string
    document: string
    name: string
    partnershipName: string
    email: string
    mobileNumber: string
    address: string
}